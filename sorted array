#include <iostream>
using namespace std;
int main() {
  int nums[5] = {1,2,3,4,5};
  int target = 4;
  
  for(int i = 0; i<5 ; i++){
      if(nums[i] == target){
          cout<<i;
          break;
      }
  }
  return 0;
}
